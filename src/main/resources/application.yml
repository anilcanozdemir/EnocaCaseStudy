server:
  port: 3000
spring:
  datasource:
    url:  jdbc:postgresql://localhost:5332/enoca
    username: anil
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true

  main:
    web-application-type: servlet